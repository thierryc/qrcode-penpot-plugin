{
  "name": "penpot-plugin-starter-template",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "preinstall": "if [ -f .nvmrc ]; then nvm use; fi",
    "dev": "vite build --watch & vite preview",
    "build": "tsc && vite build"
  },
  "author": "Thierry Charbonnel",
  "dependencies": {
    "@penpot/plugin-styles": "0.6.0",
    "@penpot/plugin-types": "0.6.0"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^5.2.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
