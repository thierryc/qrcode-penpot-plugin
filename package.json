{
  "name": "qr-code-penpot-plugin",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch & vite preview",
    "build": "tsc && vite build",
    "deploy": "gh-pages -d dist"
  },
  "author": "Thierry Charbonnel",
  "dependencies": {
    "@penpot/plugin-styles": "0.8.0",
    "@penpot/plugin-types": "0.8.0"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "typescript": "^5.2.2",
    "vite": "^5.2.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
